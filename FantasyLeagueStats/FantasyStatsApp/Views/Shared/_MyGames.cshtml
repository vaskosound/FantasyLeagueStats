@model IEnumerable<FantasyStatsApp.Models.GameBasicViewModel>

<ul>
    @foreach (var game in @Model)
    {
        <li>
            @Html.ActionLink(@game.Name, "GameDetails", new { id = @game.Id })
        </li>
    }
</ul>
